{
  "name": "root",
  "private": true,
  "scripts": {
    "preparedemo": "lerna bootstrap && lerna run build",
    "demo": "(cd packages/demo && npm start)",
    "start": "npm run preparedemo && npm run demo"
  },
  "devDependencies": {
    "@stencil/core": "1.0.0-beta.8",
    "lerna": "^3.14.1"
  },
  "dependencies": {
    "brace": "^0.11.1"
  }
}
